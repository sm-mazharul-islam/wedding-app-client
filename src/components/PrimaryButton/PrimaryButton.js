import React from "react";

const PrimaryButton = ({ children }) => {
  return (
    <div>{/* <button className="btn btn-primary">{children}</button> */}</div>
  );
};

export default PrimaryButton;
/* onk gula same button thakle akhan tekhe call kore nibo  */
